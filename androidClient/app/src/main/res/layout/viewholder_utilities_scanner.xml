<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:animateLayoutChanges="true"
    android:clickable="true"
    android:contextClickable="true"
    android:foreground="?attr/selectableItemBackground"
    app:cardBackgroundColor="?customScannerCardBackgroundColor"
    app:cardCornerRadius="4dp"
    app:cardElevation="2dp"
    app:cardUseCompatPadding="true"
    tools:ignore="UnusedAttribute">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/viewholder_scanner_format_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="12dp"
            android:text="QR Code Scanner"
            android:textColor="?customScannerCardSubTitleColor"
            android:textSize="22sp"/>

        <ImageView
            android:id="@+id/viewholder_scanner_image"
            android:layout_width="wrap_content"
            android:layout_height="100dp"
            android:layout_alignParentEnd="true"
            android:layout_alignParentLeft="true"
            android:layout_alignParentRight="true"
            android:layout_alignParentStart="true"
            android:layout_below="@+id/viewholder_scanner_format_title"
            android:layout_marginTop="8dp"
            android:foregroundGravity="center"
            android:padding="8dp"
            android:src="@drawable/ic_scanner_24dp"
            />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/viewholder_scanner_image"
            android:padding="16dp"
            android:text="For directly accessing the QR Links"
            android:textColor="?customScannerCardSubTitleColor"
            android:textSize="14sp"/>
    </RelativeLayout>
</androidx.cardview.widget.CardView>